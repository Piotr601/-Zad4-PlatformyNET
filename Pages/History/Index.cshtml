@page
@model AplikacjaWebowa.Pages.History.IndexModel

@{
    ViewData["Title"] = "Historia";
}

<h1>Historia</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Currency[0].waluta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Currency[0].kurs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Currency[0].dzien)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Currency) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.waluta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kurs)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dzien)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
